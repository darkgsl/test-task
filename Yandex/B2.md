git stash (или git stash push -m "WIP: comment")
git checkout main

# работа в main
git add filenames
git commit -m "Fix: comment"

git checkout feature/junior-task
git stash pop
git commit --amend -m "new comment" 