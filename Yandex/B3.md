1. git push
2. автоматический запуск pipeline по коммиту
3. анализ кода (Lint)
4. Unit-тесты
5. сборка Docker image c уникальным тегом 
6. Тестирование контейнера (health-check)
7. push в registry


Уведомления:
- при fail на любом этапе, пайплайн останвливается, отправляется уведомление с указанием этапа.
- при успехе, уведомление об итогах. 

Смысл в непрерывной интеграции кода небольшими порциями.Код собирается, проверяется, запускается.
На выходе всегда рабочие артефакты.
Код доставляет ся до прода: автоматически, быстро, без ошибок, верифицированно